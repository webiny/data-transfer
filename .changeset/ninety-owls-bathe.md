---
"@webiny/data-transfer": patch
---

Strip `#NNNN` revision suffixes from `id`, `parentId`, and `inheritedFrom` in FLP permission records during transfer. Fix npm 11 publish by dropping `./` prefix from bin entries. Pin `@changesets/cli` to 2.x.
