---
"@webiny/data-transfer": patch
---

Add `update-skills` CLI command for updating Claude Code skills from the installed package. Ship `.gitignore` in scaffolded projects (npm strips dotfiles, so we ship as `.gitignore.example` and rename during scaffold).
